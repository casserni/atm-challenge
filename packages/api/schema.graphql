### This file was autogenerated by GraphQL Nexus
### Do not make changes to this file directly


type accounts {
  balance: Int!
  daily_withdrawal_limit: Int!
  id: Int!
  remaining_withdrawal_limit: Int!
  transactions(skip: Int): [transactions!]!
  type: String!
  user_id: Int!
}

input IntFilter {
  equals: Int
  gt: Int
  gte: Int
  in: [Int!]
  lt: Int
  lte: Int
  not: Int
  notIn: [Int!]
}

type Mutation {
  deposit(accountId: Int!, amount: Int!): accounts
  login(pin: String!): Int
  withdraw(accountId: Int!, amount: Int!): accounts
}

type Query {
  users(skip: Int, where: QueryUsersWhereInput): [users!]!
}

input QueryUsersWhereInput {
  id: IntFilter
}

type transactions {
  account_id: Int
  amount: Int!
  id: Int!
  type: String!
}

type users {
  accounts(skip: Int): [accounts!]!
  id: Int!
  name: String!
}
